spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: qwerty
    username: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: off
  liquibase:
    enabled: true

jwt:
  token:
    security: soymun
    validate: 3600000

file:
  root: src/main/resources/root/
