spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: qwerty
    username: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: off

jwt:
  token:
    security: soymun
    validate: 3600000

file:
  root: src/main/resources/root/
